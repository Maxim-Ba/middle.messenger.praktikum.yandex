export class Validator {
  formElement: HTMLFormElement;
  constructor(formElement) {
    this.formElement = formElement;
    this._init();
  }
  checkValidity() {}
  private _init() {
    const inputs = this.formElement;
    this.formElement;
  }
}
